class Image {
	constructor(id, avatarColor, ringColor) {
		this.id = id; // ID изображения
		this.avatarColor = avatarColor; // Цвет аватара
		this.ringColor = ringColor; // Цвет кольца
	}
}

export default Image;
